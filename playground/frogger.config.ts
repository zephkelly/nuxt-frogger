import { defineFroggerOptions } from '#frogger/config';



export default defineFroggerOptions({
    app: {
        name: 'my-app',
        version: '1.0.0'
    },

    serverModule: false,
    clientModule: true,
});